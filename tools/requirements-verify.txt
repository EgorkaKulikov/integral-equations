# Окружение внешней сверки со SciPy/NumPy (задача Gradle `setupScipyVerification`).
#
# Версии зафиксированы ЖЁСТКО и намеренно. Сверка — единственная проверка проекта,
# не замкнутая на его собственный код, поэтому она обязана быть воспроизводимой:
# при плавающих версиях расхождение невозможно было бы отличить от смены поведения
# самой SciPy (менялись, например, реализации quad и linalg.solve).
#
# Обновление версий — осознанное действие: после него прогон `./gradlew scipyVerify`
# должен остаться зелёным, а новые версии — попасть в этот файл (их печатает сам
# скрипт сверки и записывает в поле `environment` машиночитаемого отчёта).
scipy==1.18.0
numpy==2.5.1
