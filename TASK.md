# Задание на разработку: верификация численных методов и устойчивость

## Контекст

Проект решает интегральные уравнения Фредгольма, Вольтерры и Урысона методом
сплайн-коллокации на минимальных сплайнах. К моменту постановки задания выполнено:

* устранены семь дефектов численных методов, каждый закрыт regression-тестом;
* формулы сверены с первичными публикациями, соответствие зафиксировано в
  [`docs/REFERENCES.md`](docs/REFERENCES.md);
* код разделён по назначению: `src/main` (ядро), `src/problems` (модельные задачи),
  `src/demo` (демонстрации, бенчмарк), `src/test`;
* создана сеть характеризационных тестов: 1316 численных значений с относительным
  допуском 1e-9 (`src/test/kotlin/characterization/`);
* текущее состояние: `./gradlew build` — успешно, 206 тестов проходят.

**Главная нерешённая проблема.** Практически все проверки замкнуты на собственную
реализацию. Характеризационный тест фиксирует, что результат *не изменился*, но не
доказывает, что он *правильный*. Если ошибка была внесена до создания эталона, она
зафиксирована вместе с ним. Настоящей независимой верификации нет.

Задание закрывает этот пробел и устраняет оставшиеся места, где система может дать
неверный результат без диагностики.

---

## Задача 1. Систематические тесты порядка сходимости

### Проблема

Порядок сходимости проверяется лишь в двух местах: `CombinedNystromTest` и
health-check базовой схемы. Для остальных сочетаний «схема × базис × семейство
функционалов» деградация точности не обнаруживается: характеризационный тест
среагирует на *изменение* значения, но не отличит ухудшение от улучшения, а при
осознанном обновлении эталона деградация будет просто зафиксирована.

Именно отсутствие такой проверки позволило дефекту `kulkarniQuasi` в решателе
Вольтерры (кусочно-линейная реконструкция срезала порядок с ~3.8 до ~1.9)
существовать незамеченным.

### Что сделать

Создать `src/test/kotlin/convergence/ConvergenceOrderTest.kt` — параметризованный
тест, проверяющий эмпирический порядок `p = log2(E_n / E_2n)` на последовательности
сеток n = 8, 16, 32, 64.

Матрица покрытия (схемы перечислены в `FredholmSecondKindSolver` и `VolterraSecondKindSolver`):

| Измерение | Значения |
|---|---|
| Уравнение | Фредгольм, Вольтерра |
| Схема | `base`, `sloan`, `kulkarni`, `iteratedKulkarni`, `nystrom`, `iteratedNystrom`, `combinedNystrom`, `iteratedCombinedNystrom` |
| Порождающая система | `B`, `H`, `T` |
| Семейство функционалов | `theta`, `xi1`, `mu`, `lambda` (для схем Nyström — только семейства без производных) |

Требования к реализации:

1. Ожидаемый порядок для каждого сочетания задать **явной таблицей** с допуском
   (предлагается ±0.4). Значения взять из фактического прогона, но **сверить с
   таблицами статьи** (см. задачу 2) и с `docs/REFERENCES.md`: там, где
   опубликованный порядок известен, расхождение более чем на 0.5 — повод для
   разбирательства, а не для подгонки допуска.
2. Задачи, решение которых лежит в `span` порождающей системы (`F2span`, `V2span`),
   исключить: там ошибка на уровне машинной точности и порядок не определён.
   Для них — отдельная проверка «ошибка ниже 1e-10».
3. При выходе на машинную точность (E < 1e-13) прекращать измельчение: дальнейший
   порядок определяется шумом округления, а не методом.
4. Сообщение об ошибке обязано содержать всю последовательность ошибок и
   вычисленные порядки, иначе диагностика невозможна.

### Критерий приёмки

Тест проходит; при искусственном внесении дефекта (например, возврат к
кусочно-линейной реконструкции в `kulkarniQuasi`) — падает с внятным сообщением.
Проверить это фактически, временно внеся регрессию.

---

## Задача 2. Независимая верификация (ключевая задача)

### Проблема

Все текущие эталоны получены той же реализацией, которую проверяют. Нужны источники
истины, **не зависящие** от кода проекта. Ниже пять независимых направлений,
упорядоченных по убыванию доказательной силы. Реализовать следует **все**.

### 2.1. Аналитически точные решения (сильнейший эталон)

**Вырожденные (сепарабельные) ядра.** Если `K(t,s) = Σ aᵢ(t) bᵢ(s)` — конечная
сумма, уравнение Фредгольма второго рода сводится к **конечной** системе линейных
уравнений, решаемой точно в замкнутой форме, без какой-либо дискретизации.

Пример: `K(t,s) = t·s`, `λ = 1`, `f(t) = t`. Подстановка `u(t) = f(t) + λ·c·t`, где
`c = ∫₀¹ s·u(s) ds`, даёт скалярное уравнение на `c` с точным ответом.

Реализовать не менее трёх задач с сепарабельным ядром (ранг 1, 2 и 3) в
`src/problems/kotlin/problems/analytic/` и проверить, что численное решение сходится
к аналитическому. Это проверяет всю цепочку — базис, функционалы, квадратуру, сборку
матрицы и решение СЛАУ — против результата, полученного **вручную на бумаге**.

**Уравнение Вольтерры с ядром свёртки.** Для `K(t,s) = k(t−s)` уравнение решается
преобразованием Лапласа аналитически. Пример: `K(t,s) = 1`, `λ = 1`, `f(t) = 1` даёт
`u(t) = e^t`. Добавить не менее двух таких задач.

**Метод производных решений (method of manufactured solutions).** Задать желаемое
решение `u*`, подставить в уравнение и вычислить правую часть `f` аналитически (не
квадратурой!). Сейчас `f` строится численным интегрированием, из-за чего ошибка
квадратуры входит и в эталон, и в решение, частично взаимно компенсируясь. Задача с
аналитически выписанной `f` разрывает эту связь.

### 2.2. Эталонные значения из публикации

В каталоге статьи `Scientific Agents/papers/new-01/experiments/tables/` находится
**18 таблиц** с числовыми результатами, помеченных «Auto-prepared from verified
runs. Do not alter numbers»: `table-t1-f2.tex`, `table-t1-v2.tex`,
`table-t2-fredholm.tex`, `table-t3-volterra.tex`, `table-xi-*.tex`, `table-f1.tex`,
`table-v1.tex` и другие.

Извлечь из них значения `E_h` и порядки `p_h` и оформить как тест соответствия
опубликованным результатам. Значения (например, для F2/базис B: `1.014e-4`,
`1.246e-5`, `1.521e-6`, `1.871e-7` при n = 8, 16, 32, 64) поместить в ресурсный файл
с указанием таблицы-источника.

Допуск — не строже 2 % (в публикации четыре значащие цифры). Расхождение более 2 %
означает либо расхождение с опубликованным результатом, либо ошибку переноса — и
то, и другое требует явного разбирательства и фиксации в отчёте, а не ослабления
допуска.

### 2.3. Сравнение с независимой реализацией

Реализовать **простейший метод Nyström с квадратурой Гаусса–Лежандра**, не
использующий ни сплайнов, ни функционалов проекта: узлы квадратуры, матрица
`(I − λ·w_j·K(t_i,t_j))`, решение СЛАУ. Порядка 40 строк кода в тестовом наборе.

Это классический метод из учебника (Atkinson 1997, гл. 4), полностью независимый от
проверяемого кода. На гладких задачах он даёт высокую точность и служит внешним
эталоном. Разместить в `src/test/kotlin/verification/ReferenceNystromSolver.kt` с
пометкой, что это эталон для сверки, а не часть библиотеки.

Дополнительно (если доступен Python с SciPy): подготовить скрипт
`tools/verify_with_scipy.py`, решающий те же задачи средствами `scipy.integrate` и
печатающий значения для ручной сверки. Скрипт не должен входить в сборку Gradle;
его назначение — разовая внешняя проверка, результат которой фиксируется в отчёте.

### 2.4. Перекрёстная проверка схем

Разные схемы решают одну задачу и обязаны сходиться к одному пределу. Проверить, что
при n = 64 результаты `base`, `sloan`, `kulkarni`, `nystrom`, `combinedNystrom`
попарно согласованы в пределах, определяемых их порядками (различие не превосходит
величины наибольшей из ошибок, умноженной на разумный множитель).

Это не доказывает правильность, но обнаруживает ситуацию, когда одна схема
рассогласована с остальными.

### 2.5. Property-based проверки (математические инварианты)

Свойства, которые обязаны выполняться при **любых** входных данных; проверить на
случайных, но воспроизводимых (фиксированный seed) наборах:

| Инвариант | Формулировка |
|---|---|
| Линейность | решение для `f₁ + c·f₂` равно сумме решений с тем же коэффициентом |
| Нулевая правая часть | при `f ≡ 0` решение тождественно нулевое |
| Точность на span | если `u*` лежит в span порождающей системы — ошибка машинная |
| Разбиение единицы | `Σ omega_j(t) = 1` в любой точке отрезка |
| Биортогональность | `chi_i(omega_j) = delta_ij` для всех проекторов |
| Идемпотентность | `P(P g) = P g` для семейств с `isProjector = true` |
| Согласованность оператора | `applyNodes` совпадает с `apply` в узлах квадратуры |

Часть инвариантов уже проверяется в `healthchecks` на фиксированных данных — их
следует расширить на случайные входы, а не дублировать.

### Критерий приёмки задачи 2

Все пять направлений реализованы; тесты проходят. Расхождения (если обнаружатся)
задокументированы с указанием величины и вероятной причины — **скрывать расхождение
ослаблением допуска запрещено**. По итогам обновить `docs/REFERENCES.md`: для
каждого метода указать, какими независимыми средствами он проверен.

---

## Задача 3. Проверка условий применимости (Вольтерра, первый род)

### Проблема

`VolterraFirstKindSolver` в `src/main/kotlin/solvers/volterra/VolterraSolver.kt` проверяет
условие `K(t,t) ≠ 0` только в узлах сетки и серединах интервалов
(`KERNEL_DIAGONAL_TOLERANCE`, строка ~561). Фактически деление на `K(t,t)`
происходит:

* в `gEff` — во **всех** гауссовых узлах квадратуры;
* внутри `deriv4` — в точках `t ± k·FINITE_DIFFERENCE_STEP`, которые не совпадают
  ни с узлами, ни с серединами.

Ядро, обращающееся в ноль между проверенными точками, даст `NaN` или `Inf` без
диагностики: пользователь получит бессмысленный результат, не узнав об этом.

### Что сделать

1. Расширить проверку на точки, где деление реально выполняется: гауссовы узлы
   составной квадратуры и шаблон конечной разности.
2. Добавить защиту в самих точках деления: при обнаружении значения ниже порога —
   исключение с указанием точки `t` и величины `K(t,t)`, а не молчаливый `NaN`.
3. Проверить, не выходит ли шаблон `t ± 4·FINITE_DIFFERENCE_STEP` за пределы
   отрезка при малой его длине (`b − a` порядка `1e-3`). Константа абсолютная; если
   выход возможен — сделать шаг относительным к длине отрезка либо явно запретить
   такие отрезки с внятным сообщением.
4. Regression-тест: ядро, положительное в узлах и серединах, но обращающееся в ноль
   между ними, обязано вызывать исключение, а не возвращать `NaN`.

---

## Задача 4. Единая диагностика расходимости

### Проблема

Итерационные методы ведут себя по-разному при недостижении сходимости:

| Метод | Поведение сейчас |
|---|---|
| `combinedNystrom` (оба решателя) | бросает `IllegalStateException` — **правильно** |
| `kulkarniQuasi` (Фредгольм, строка ~252) | молча возвращает последний итерант |
| `kulkarniQuasi` (Вольтерра) | молча возвращает последний итерант |
| Ньютон в решателе Урысона | записывает предупреждение в лог и возвращает результат |
| Гаусс–Ньютон (Урысон, первый род) | записывает предупреждение в лог |

Пользователь библиотеки не может отличить сошедшийся результат от расходящегося:
предупреждение в логе при программном использовании не будет замечено.

### Что сделать

Привести к единому, явному контракту:

1. Ввести признак сходимости в возвращаемом значении. Решение о форме — за
   исполнителем; допустимые варианты: поле `converged: Boolean` и `iterations` в
   `SolutionFunc`, либо отдельный тип результата. Выбранный вариант обосновать.
2. Поведение по умолчанию — исключение при недостижении сходимости (как в
   `combinedNystrom`): молчаливый возврат неверного результата недопустим.
3. Предусмотреть режим, позволяющий получить неcошедшийся результат осознанно
   (например, параметр `throwOnDivergence: Boolean = true`) — это нужно для
   исследовательских сценариев и для тестов.
4. Логирование сохранить как дополнение, а не как единственный канал сигнала.
5. Тесты: задача, заведомо не дающая сходимости за отведённое число итераций,
   должна приводить к исключению; при отключённом режиме — к результату с
   `converged = false`.

**Внимание к совместимости.** Изменение публичного API затрагивает `src/demo`,
`src/problems` и тесты. Численные результаты меняться **не должны**:
`characterization.EhCharacterizationTest` обязан проходить без обновления эталона.

---

## Задача 5. Чистка остатков

Небольшие изменения, повышающие связность. Каждое проверить `find_usages` перед
удалением.

1. **`numerics/CheckResult.kt`** — после превращения health-checks в тесты класс
   более не используется production-кодом: единственные обращения находятся в
   `CheckResultTest` и `UrysonCoverageTest`, то есть он тестирует сам себя.
   Удалить вместе с тестами.

2. **`numerics/Fmt.kt`** — используется только в `src/demo` (три файла
   демонстраций). Форматирование чисел и генерация LaTeX-разметки не относятся к
   вычислительному ядру. Перенести в `src/demo/kotlin/demo/format/`, после чего
   убрать соответствующее исключение из настройки Kover в `build.gradle.kts`.

3. **`numerics/backend/MultikCpuBackend.kt`** — сначала **измерить**, затем менять.
   Методы `fromD2`/`fromD1` читают результат поэлементно с упаковкой в объекты,
   `addScaled` выполняет три аллокации `NDArray` ради операции, стоящей одного
   прохода по массиву. Вероятно, на малых размерностях реализация на чистом JVM
   быстрее нативной.

   Добавить в бенчмарк (`src/demo/kotlin/demo/bench/Benchmark.kt`) сценарий
   сравнения бэкендов на размерностях 16, 64, 256, 1024 по операциям `matMat`,
   `matVec`, `addScaled`, `solve`. По результатам решить: оптимизировать
   преобразования, ввести порог переключения на JVM-реализацию для малых
   размерностей либо оставить как есть. Решение обосновать измерениями.

---

## Общие требования

1. **Контроль регрессий.** Перед изменением, способным затронуть численные
   результаты, снять эталон: `./gradlew captureBaseline`. После изменения
   `characterization.EhCharacterizationTest` обязан проходить. Если расхождение
   обосновано, обновить эталон осознанно и привести в отчёте старые и новые
   значения. Подгонка допусков вместо разбирательства недопустима.
2. **Комментарии и документация — на русском языке.** Названия публикаций,
   методов и программные идентификаторы сохраняются в оригинале.
3. **Ссылки на источники.** Новые методы и эталоны сопровождать ссылкой на
   первичную публикацию в `docs/REFERENCES.md`. Если источника нет — пометить
   явно, не придумывая.
4. **Каждое исправление ошибки** сопровождается тестом, падающим на коде до
   исправления. Факт падения проверить, а не предположить.
5. **Порядок работ.** Задачи 1 и 2 — приоритетные и независимы друг от друга,
   можно вести параллельно. Задача 4 меняет публичный API, поэтому её следует
   выполнять после 1 и 2, когда сеть проверок плотнее. Задачи 3 и 5 независимы.
6. Не расширять область работ за пределы задания. Обнаруженные попутно проблемы
   фиксировать в отчёте, а не исправлять молча.

## Критерии завершения

* `./gradlew build` завершается успешно; все тесты проходят.
* Порядки сходимости всех схем проверяются автоматически и согласованы с
  опубликованными значениями.
* Каждый численный метод проверен **минимум двумя независимыми** способами из
  задачи 2, и в `docs/REFERENCES.md` указано, какими именно.
* Уравнение Вольтерры первого рода не может молча вернуть `NaN`.
* Ни один итерационный метод не возвращает неcошедшийся результат без явного
  признака.
* В `numerics` не осталось кода, не относящегося к вычислениям.
* Решение по бэкенду линейной алгебры подкреплено измерениями.

## Отчёт по завершении

1. Что проверено и какими независимыми средствами — таблица «метод → способы
   верификации → результат».
2. Обнаруженные расхождения: величина, причина, принятое решение.
3. Изменения публичного API и их обоснование.
4. Фактические результаты: сборка, тесты, бенчмарк сравнения бэкендов.
5. Что осталось непроверенным и почему.
