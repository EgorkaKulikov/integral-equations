# Versions of the libraries the project is built against. Both are consumed as
# published Maven artifacts (mavenLocal or the repository given by numericsRepositoryUrl).
numericalCoreVersion=1.1.0
minimalSplinesVersion=1.1.0
org.gradle.jvmargs=-Xmx2g
